apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team-red-dev
  annotations:
    kargo.akuity.io/authorized-stage: "team-red:dev"
spec:
  project: default
  source:
    repoURL: https://github.com/dhpup/akuity-mcp-demo.git
    targetRevision: main
    path: teams/team-red/dev
  destination:
    name: mac1
    namespace: team-red-dev
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
