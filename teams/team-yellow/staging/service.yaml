apiVersion: v1
kind: Service
metadata:
  name: guestbook
  labels:
    app: guestbook
    team: yellow
    env: staging
spec:
  type: ClusterIP
  selector:
    app: guestbook
    team: yellow
  ports:
    - name: http
      port: 80
      targetPort: 3000
